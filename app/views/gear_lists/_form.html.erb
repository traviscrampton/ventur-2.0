<div id="gear_form">
  <div class="wrapper center">
		<%= form_for(@gear_list) do |f| %>
			<%= f.text_field :title, placeholder: "Name this list", autofocus: true %>
			<%= f.submit "Create Gear List", class: "button button_fade" %>
		<% end %>
	</div>
</div>